<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Launcher" parent="android:Theme.Wallpaper.NoTitleBar.Fullscreen">
        <!--<item name="android:windowNoTitle">true</item>-->
    </style>

    <style name="LauncherTheme" parent="@android:style/Theme.DeviceDefault.Wallpaper.NoTitleBar"></style>

    //////////////
    <style name="BaseWallpaperTheme" parent="@android:style/Theme.DeviceDefault.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowShowWallpaper">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="Theme" parent="@style/BaseWallpaperTheme"></style>

</resources>
